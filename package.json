{
  "private": true,
  "name": "eppendorf",
  "version": "1.0.0",
  "description": "demo repository",
  "main": "index.js",
  "scripts": {
    "test": "yarn --cwd server test",
    "watch": "concurrently --kill-others 'yarn --cwd server watch' 'yarn --cwd client start'"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "author": "jm",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
