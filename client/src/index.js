import React from 'react';
import ReactDOM from 'react-dom';
import DeviceStateList from './DeviceStateList';

ReactDOM.render(<DeviceStateList />, document.getElementById('root'));

// extraced from example code
// @TODO use a dedicated router / react-router